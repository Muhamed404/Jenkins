pipeline {
  agent any
  
  stages {
    stage('Set Variable') {
      steps {
        script {
          sh '''
            docker build -t my-nginx-webapp .
            docker run -d -p 9000:80 --name my-nginx-webapp-container my-nginx-webapp
          '''
        }
      }
    }
  }
}
